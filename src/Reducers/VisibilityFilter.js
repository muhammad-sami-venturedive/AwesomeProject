export default VisibilityFilter = (state = "SHOW_ALL" , action) => {
    switch (action.type) {
        case 'value':
            
            break;
    
        default:
            return state;
    }

}